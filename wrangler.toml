name = "remaker-ai"
main = "worker.js"
compatibility_date = "2024-09-23"

# Serve i file statici dalla cartella del progetto
[assets]
directory = "."

# La chiave Gemini NON va qui — usare il dashboard Cloudflare:
# Workers & Pages → remaker-ai → Settings → Variables and Secrets → GEMINI_API_KEY
